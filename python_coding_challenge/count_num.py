# Givenlist 
lst = [10, 60, 30, 80]
# Counter var, initialize by 0
count = 0
# Loop for list
for num in lst:
    if num > 50:
        count += 1   # Increase count by 1
# Printingresult
print(count)